分布式配置中心
    不懂的地方： git仓库的配置，作用为何
回想一下：
```
    有一个客户端需要将配置文件迁移到配置中心
    本地的配置文件都不需要了，只有配置中心有配置文件
    组件1，2，...，n的配置文件都在一起，可以集中管理，
    需要修改那个属性，进行手动管理
    git仓库的配置，只是指定配置文件放在git仓库中，并被git版本控制器管理
```


本节所作的工作：
- 构建config-server,连接到git仓库
- 创建config-repo目录，用来存储配置信息
- 构建config-client,来获取git-client配置信息
